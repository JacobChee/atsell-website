import React from 'react';
import ReactDOM from 'react-dom/client';
import AtsellLanding from './AtsellLanding';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <AtsellLanding />
  </React.StrictMode>
);
